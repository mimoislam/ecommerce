import 'package:flutter/material.dart';

class ProductSizeColor{
  String color,size;
  Color hexColor;
  ProductSizeColor({

    required this.hexColor,
    required this.color,
    required this.size,
  });
}